class assassinGraph extends graph {
    
    constructor(numOfPlayers) {
        numOfVertices = numOfPlayers;
        super(numOfVertices);
        super(AdjList);   
    }

    eliminatePlayer(k, t) {
        
    }
}